// const express = require("express");
// const router = express.Router();
// const claimController = require("../controllers/claimController");

// router.post("/create", claimController.createClaim);
// // router.get("/user/:userId", claimController.getUserClaims); // will be used by admin later
// // router.get("/:claimId", claimController.getClaimById); // will be used by admin later
// // router.put("/:claimId", claimController.updateClaim); // will be used by admin later
// // router.delete("/:claimId", claimController.deleteClaim); // will be used by admin later

// module.exports = router;
